# TASK-3
The Titanic dataset is first loaded using Pandas to examine its structure by displaying the initial and final records. By inspecting column names and values, features are manually classified into numerical (Age, Fare), categorical (Sex, Embarked), ordinal (Pclass), and binary (Survived). The df.info() and df.describe() functions are used to analyze
